# Инструкция по запуску Локального Космолета 🚀
Версия: 1.1.0

Этот инструмент позволяет обрабатывать любые Excel-файлы локально с помощью ИИ DeepSeek, не отправляя данные в облако.

## 1. Установка Ollama (Ваш локальный ИИ)
1. Скачайте и установите Ollama с [ollama.com](https://ollama.com).
2. После установки откройте терминал (или командную строку) и введите:
   ```bash
   ollama run deepseek-r1:8b
   ```
   (Это один раз скачает модель DeepSeek на ваш компьютер).

## 2. Настройка Python
У вас должен быть установлен Python 3.10 или новее.

1. Установите необходимые библиотеки:
   ```bash
   pip install -r requirements.txt
   ```

## 3. Запуск приложения
В папке проекта выполните:
```bash
streamlit run app.py
```

## Как это работает на Windows и Mac
- Код абсолютно одинаков для обеих систем.
- На Windows рекомендуется использовать PowerShell или VS Code Terminal.
- На Mac (M1/M2/M3/M4) обработка будет идти максимально быстро за счет Neural Engine.
